using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScreenFlip : MonoBehaviour
{
    void Awake()
    {
//#if UNITY_WEBGL && !UNITY_EDITOR
        // var scale = transform.localScale;
        // scale.y *= -1;
        // transform.localScale = scale;
//#endif
    }
}
